1_📨_Email.py:

обзор скрипта:

Импорт необходимых пакетов: скрипт импортирует необходимые пакеты, включая pandas, streamlit, и streamlit_authenticator.

Определение конфигурации страницы: скрипт задает страницу с потоковым воспроизведением

Определение функций: Скрипт включает в себя несколько

update_data(): Это
loud_data(df): Эта функция преобразует фрейм данных в файл Excel и возвращает его в виде байтов.
filter_dataframe(df): Эта функция фильтрует фрейм данных сообщения на основе определенных критериев (не показаны в предоставленном коде
Аутентификация и авторизация пользователя: скрипт использует streamlit_authenticator для обработки аутентификации и авторизации пользователя. Он проверяет предоставленные имя пользователя и пароль по списку действительных пользователей (get_users()
Макет приложения: сценарий

"Статус сообщений" section: Displays the status of email messages,get_count() в формате фрейма данных.
раздел "Сообщения": отображает сообщения электронной почты, извлеченные из базы данных с помощью get_data(), в формате DataFrame. Он также включает кнопку загрузки для загрузки сообщений в формате Excel.
Стиль Streamlit:

Статус аутентификации пользователя: скрипт скрывает определенные элементы и отображает определенные сообщения на основе статуса аутентификации пользователя.

Выход:authenticator.logout(), которое позволяет

В целом, скрипт обеспечивает простой

Примечание: Предоставленный код использует несколько пользовательских функций и компонентов (get_users()get_data()get_count() и filter_dataframe()), которые не включены в предоставленный код

filtor.py:

скрипт на Python - это служебная функция, которая добавляет пользовательский интерфейс поверх фрейма данных pandas, чтобы зрители могли фильтровать его столбцы. Функция использует виджеты Streamlit для предоставления интерактивных параметров фильтрации для различных типов данных в DataFrame.

Вот обзор функции:

Импорт необходимых пакетов: скрипт импортирует необходимые пакеты, включая pandas и streamlit.

Определение функции: Функция filter_dataframe(df: pd.DataFrame) -> pd.DataFrame определена для интерактивной фильтрации данного фрейма данных.

Пользовательский интерфейс для добавления фильтров: функция отображает флажок с именем "Добавлять фильтры" с помощью st.checkbox. Если этот флажок установлен, фрейм данных будет изменен на основе пользовательских настроек фильтрации. Если флажок не установлен, будет возвращен исходный фрейм данных.

Изменение фрейма данных: если пользователь устанавливает флажок "Добавить фильтры", функция создает копию исходного фрейма данных. Затем оно пытается преобразовать столбцы с типом объекта dtype в тип даты и времени dtype с помощью pd.to_datetime, если это возможно. Оно также преобразует столбцы datetime с информацией о часовом поясе в столбцы datetime без использования часового поясаdt.tz_localize(None).

Параметры фильтрации: Функция предоставляет различные параметры фильтрации на основе типа данных столбцов в DataFrame:

Категориальные столбцы: Если столбец является категориальным (или имеет менее 10 уникальных значений), пользователь может выбрать несколько значений из этого столбца с помощью multiselect, и фрейм данных будет отфильтрован, чтобы включить только строки с выбранными значениями в этом столбце.

Числовые столбцы: Если столбец является числовым, пользователь может использовать ползунок для выбора диапазона числовых значений для этого столбца с помощью slider, и фрейм данных будет отфильтрован, чтобы включать только строки с числовыми значениями в пределах выбранного диапазона в этом столбце.

Столбцы Datetime: Если столбец имеет тип данных datetime, пользователь может использовать средство выбора даты, чтобы выбрать диапазон дат для этого столбца с помощью date_input, и фрейм данных будет отфильтрован, чтобы включать только строки со значениями datetime в пределах выбранного диапазона в этом столбце.

Другие столбцы: Если столбец имеет любой другой тип данных, пользователь может использовать текстовый ввод для ввода подстроки или регулярного выражения, и фрейм данных будет отфильтрован, чтобы включать только строки, строковое представление столбца содержит ввод пользователя.

Результат фрейма данных: отфильтрованный фрейм данных возвращается в качестве выходных данных функции.

В целом, эта служебная функция предоставляет удобный способ интерактивной фильтрации фреймов данных pandas с использованием пользовательского интерфейса на основе Streamlit. Оно позволяет пользователям просматривать и анализировать данные, выбирая определенные значения или диапазоны для разных столбцов, что делает поиск данных более удобным и эффективным.

generate_keys.py:

Приведенный фрагмент кода демонстрирует, как использовать streamlit_authenticator пакет для хэширования паролей. streamlit_authenticator.Hasher Класс используется для хэширования списка паролей, а сгенерированные хэшированные пароли выводятся на консоль.

Вот объяснение кода:

Импортstreamlit_authenticator: код импортирует streamlit_authenticator пакет, который предоставляет функциональные возможности для аутентификации пользователей в потоковых приложениях.

Создайте список паролей: код создает список паролей для хэширования. В этом примере список содержит два пароля: 'abc' и 'def'. При необходимости вы можете добавить дополнительные пароли к этому списку.

Генерация хэшированных паролей: streamlit_authenticator.Hasher класс инициализируется списком паролей, а generate() метод вызывается для генерации хэшированных версий паролей. Сгенерированные хэшированные пароли хранятся в словаре, где исходные пароли являются ключами, а их соответствующие хэшированные версии - значениями.

Печать хэшированных паролей: Наконец, код выводит сгенерированные хэшированные пароли на консоль.

Пример вывода:

{
    'abc': 'sha256$yFyfGp7P$84836e60ab3d3c9d4e44efc2fc0d45f9e1270ff0b33be7b53e9dce4da4d56c40',
    'def': 'sha256$dyPDXkj7$702f4e07982540b20a9e66a1a71a9e68f55fc65d48b8ae6f8d9281a617853cb0'
}
Каждый пароль в списке хэшируется с использованием алгоритма SHA-256 со случайной солью, что делает пароли более безопасными. Хэшированные пароли могут храниться в базе данных или любом другом хранилище, которое будет использоваться для аутентификации пользователя в будущем. Когда пользователь пытается войти в систему, введенный им пароль может быть хэширован с использованием того же алгоритма, а затем сравнен с сохраненным хэшированным паролем для проверки его учетных данных.

GetData.py:

Предоставленный код содержит функции доступа к базе данных, использующие SQLAlchemy для взаимодействия с базой данных SQLite. Вот объяснение функций:

get_data() функция: Эта функция извлекает сообщения из базы данных. Она запрашивает Message таблицу, упорядочивает сообщения по убыванию id и ограничивает результат последними 10 000 сообщениями. Возвращаемая Messages переменная будет содержать объект запроса SQLAlchemy.

get_count() функция: Эта функция извлекает количество сообщений в разных статусах (отправлено, ошибка и поставлено в очередь). Оно использует count() функцию SQLAlchemy для подсчета сообщений с разными значениями статуса и возвращает список словарей, содержащих значения для каждого статуса.

get_users() функция: Эта функция извлекает пользовательские данные из базы данных. Она запрашивает User таблицу, чтобы получить все записи пользователя. Возвращаемой Users_data переменной будет словарь с пользовательской информацией. Ключ каждого пользователя username служит ключом, а значением является другой словарь, содержащий поля email, name и password для этого пользователя.

В целом, эти функции позволяют вам взаимодействовать с базой данных для извлечения сообщений, подсчета количества сообщений и извлечения информации о пользователе.

Пожалуйста, обратите внимание, что код использует функции запросов SQLAlchemy, такие как query, filter, count и limit для взаимодействия с базой данных. Фактические операции с базой данных будут выполняться при вызове этих функций в вашем приложении.

model.py:

Предоставленный код определяет две таблицы SQLAlchemy, message и User вместе с соответствующими классами моделей Message и User. Вот объяснение кода:

metadata: metadata является экземпляром MetaData, который используется для хранения структурной информации о таблицах базы данных.

mapper_registry: mapper_registry является экземпляром registry, который используется для отслеживания картографов SQLAlchemy для моделей.

message таблица: message Таблица определяется со следующими столбцами:

id: Столбец первичного ключа Integer.
to: Столбец String длиной 200 символов, представляющий получателя сообщения.
subject: Столбец String длиной 200 символов, представляющий тему сообщения.
message: Текстовый столбец, представляющий содержимое сообщения.
type: Столбец String длиной 200 символов, представляющий тип сообщения.
status: Целочисленный столбец, представляющий статус сообщения.
system: Столбец String длиной 200 символов, представляющий систему, связанную с сообщением.
datatime: Столбец DateTime, представляющий временную метку сообщения.
user таблица: user Таблица определяется со следующими столбцами:

id: Столбец первичного ключа Integer.
name: Столбец String длиной 64 символа, представляющий имя пользователя.
username: Столбец String длиной 64 символа, представляющий имя пользователя. Он уникален и проиндексирован.
email: Столбец String длиной 64 символа, представляющий адрес электронной почты пользователя.
password: Столбец String длиной 128 символов, представляющий хэшированный пароль пользователя.
Message и User классы: это простые классы-заполнители. Используя mapper_registry.map_imperatively() метод, SQLAlchemy автоматически сопоставляет классы моделей с соответствующими таблицами базы данных.

вкратце, предоставленный код настраивает необходимые структуры таблиц и классы моделей для message и User таблиц в SQLAlchemy. Эти структуры могут использоваться для выполнения операций с базой данных и взаимодействия с базой данных SQLite, которая указана в create_engine функции в другом месте приложения.